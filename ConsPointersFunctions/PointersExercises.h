#pragma once

// 1. Swap the pointers of the references.
void SwapPtrs(int* &a, int* &b);

// 2. Use the starting and ending address of an array to determine the length of the array
int DistanceBetweenPointers(int* pa, int* pb);


